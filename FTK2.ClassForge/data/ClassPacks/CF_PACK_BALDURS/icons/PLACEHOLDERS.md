# Placeholder art

All PNGs in `icons/` and `portraits/` are 1×1 placeholder images so the pack structure is complete
and loadable. Replace with real art before release (reference dimensions: EOR ships class portraits
~1–1.8MB PNGs and trait icons of varying sizes; match the base-game aspect ratios).
