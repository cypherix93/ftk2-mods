namespace TypeProbe
{
    // Placeholder entry point so this Exe project builds standalone in Task 2.
    // Replaced by the real CLI in Task 3 Step 7.
    internal static class Program
    {
        internal static int Main(string[] args)
        {
            return 0;
        }
    }
}
